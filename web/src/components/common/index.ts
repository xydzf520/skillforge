/**
 * 公共状态组件 — 统一 Loading / Empty / Error 展示
 *
 * 用法：
 *   import { SfLoadingState, SfEmptyState, SfErrorState } from '@/components/common'
 */

export { default as SfLoadingState } from './SfLoadingState.vue'
export { default as SfEmptyState } from './SfEmptyState.vue'
export { default as SfErrorState } from './SfErrorState.vue'
