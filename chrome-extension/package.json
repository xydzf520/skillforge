{
  "name": "skillforge-cookie-sync-extension",
  "private": true,
  "version": "1.1.1",
  "type": "module",
  "scripts": {
    "build": "esbuild build.ts --platform=node --packages=external --format=esm --outfile=.build/build.mjs && node .build/build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "esbuild": "^0.25.11",
    "typescript": "^5.9.3"
  }
}
