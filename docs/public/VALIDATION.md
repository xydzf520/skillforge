# 公开准备版验证记录

验证日期：2026-09-20。本记录仅覆盖脱敏与本地运行准备，不代表企业外部系统、真实训练或全部功能验收。

## 清理范围

- 完整原仓库保留在私有目录，原始跟踪文件未修改。
- 公开树独立导出，未复制 `.git`、原远程地址和历史作者信息。
- 排除人员与业务数据、内部部署及运营文档、真实截图、运行报告、第三方受限运行时和生成的前端包。
- 企业专有标识、已知员工标识、内部部署 URL 的复核未发现剩余命中。
- Gitleaks 8.30.1 文件树扫描通过。6 处经人工核实的配置键、枚举或合成测试误报使用逐行注释说明，没有对整个测试目录设置扫描豁免。
- 自有公开分发检查通过；新增提交钩子读取暂存区内容，CI 检查文件树及完整 Git 历史。扫描不能替代最终内容审阅。

## 实际验证

| 项目 | 结果与范围 |
|---|---|
| 后端定向回归 | 420 个不同用例通过；调度、认证、编程助手环境隔离、Bridge 训练操作、训练任务/资源/数据集、CLI、路径校验、服务注册及公开版安全初始化 |
| Web 测试 | 474 通过、1 跳过；64 个测试文件，无未处理异常 |
| Web | TypeScript/Vue 类型检查、生产构建通过 |
| 流程编辑器 | 类型检查、生产构建通过 |
| Chrome 扩展 | 类型检查、生产构建通过；构建脚本不再依赖 Node 编译时可选的 TypeScript stripping |
| 浏览器部署辅助代码 | TypeScript 检查通过 |
| Python | app、bridge、scripts、migrations 编译检查通过 |
| 空库安装 | 独立 PostgreSQL 18.6 上从空库迁移至 `138_media_review_wall_v41`，管理员初始化通过 |
| 本地启动 | 独立 Redis 8.0.5，后端 `/health` 返回 ok，生产网页入口 HTTP 200；正常关闭 |
| 默认行为（初始验证） | 没有通用管理员密码；第三方运行时、平台后端系统服务自动注册、连续素材创建、自动训练及部署审批默认关闭。Bridge 独立安装程序不适用该自动注册说明 |

测试使用随机临时凭据、隔离数据库和合成数据；未调用企业系统、未消耗真实模型额度、未向企业节点派发任务。测试服务已在验证结束后停止。

## 本次修复的可迁移性问题

- 初始数据库迁移遗漏 `system_config`，干净环境运行到后续种子迁移会报错；已补到独立公开版的初始迁移。
- 个人路径、固定部署节点地址与从节点编号推算 IP 改为本地相对路径或显式配置。
- 原私有 AI 门户的 SSO / OIDC 和身份转发已整体移除，配置和桌面／移动端入口一起删除；旧接口在存在生产网页资源时仍返回 404。平台自身账号密码及钉钉登录定向回归通过。
- 素材连续生产必须配置项目和截止时间，未配置时保持关闭。
- 前端测试补齐组件模拟，并以 4 个工作线程避免过度并发产生超时；修复图表测试的浏览器能力缺失。
- 训练自动化用例显式开启测试策略，默认配置继续保持关闭。

## 尚未覆盖

- 初始阶段未执行全部后端测试；后续全量试跑发现失败并中止，详见[最新发布复核](RELEASE_CHECK_20260920.md)，完整 CI 尚未通过。
- 本机没有 Docker，未实际构建容器镜像；Compose 目标 PostgreSQL 15 / Redis 7 还需在对应版本验收。
- 外部模型、真实钉钉服务、浏览器会话、训练 GPU 与模型部署尚未验证；本地钉钉登录回归使用合成数据。
- 初次依赖审计告警已在后续升级中处理；当前安装版本的 npm / pip 扫描为零已知告警，见[最新发布复核](RELEASE_CHECK_20260920.md)。
- 本准备版面向全新部署，不用于直接覆盖既有企业数据库；原始企业版本保持不变。
- 项目现已选用 Apache-2.0；公司成果的权属和可公开范围仍需核对。受限第三方运行时已经排除，不等于整个项目的发布授权已完成。

详细执行日志与含原始路径的迁移清单保存在私有审计目录，不随公开树分发。

## 双语 README 与收件人配置补充验证 · 2026-09-20

- 默认中文 `README.md` 与英文 `README.en.md` 均覆盖产品形态、企业应用、人机协作、Skill 编写、资产积累和未来判断；未来方向与已有能力分开说明。
- 两份 README 与文档索引的本地链接检查通过；两份 Mermaid 流程图解析通过；中英文启动与验证命令一致。
- 阅读协作文档时补充发现固定收件人个人信息，已替换文档和测试中的标识，并将生产路由改为部署配置。默认留空时不查找用户、不产生该业务通知；仅配置 ID 时不会因空姓名匹配其他用户。
- 收件人配置、节点调度、执行结果、钉钉收件人及公开分发检查等定向回归 **104 通过、1 排除**。排除项依赖公开版不分发的独立业务 Skill 文件，不能计为通过。
- Codex 网关组织查询、缓存联系人和收件人关联回归 **5 通过**。测试只使用隔离数据库与合成身份，未发送真实消息。
- 本节为补充验证，不表示重新运行了上表全部测试或完成外部系统验收。

## Harness、数据训练流程与产品判断文档补充 · 2026-09-20

- 中英文 README 增加自建 Harness 集成、数据来源与流转、样本和数据集版本、训练与评估、部署和回滚，以及业务资产和私有小模型的产品方向。
- 对照 Project SDK/API、学习事件与提取服务、训练模型与服务、Bridge 本地运行器及执行时模型选择代码核对描述；没有将需要适配的外部 Harness、通用自动择模或完整业务评估写成已验收能力。
- 两种语言的 10 个章节、导航锚点和启动／验证命令一致；本地文档链接、6 个 Mermaid 图的语法解析、公开分发检查及 `git diff --check` 通过。
- 本次仅修改文档，未重新运行后端回归或真实训练，也未调用外部模型服务。

## 全项目产品文档查漏 · 2026-09-20

- 核对主服务路由、网页路由及 22 类能力的关键模型和实现，补充中英文能力地图、业务交付、个人界面、提示词、素材、治理、成本与运营说明；同时更新架构和项目展示文档。
- 逐路径复核提示词默认选择、优化器候选评测和模拟回退、副模型检查、个人界面保存恢复及数据库备份。将代码支持、需配置能力、未来方向和待修复限制分开记录，未把静态阅读标为真实生产验收。
- 中英文 README 的 10 个章节、导航与启动／验证命令一致；新增能力地图按同一 22 类模块编排。本次变更文档的本地链接和锚点、8 个 Mermaid 图的解析、公开分发检查及差异空白检查通过。
- 本次为文档变更，无运行时代码修改；未调用外部业务系统、模型、通知或训练服务，未重跑全套回归。列出的工程限制仍待单独修复和测试。

## 旧编程运行时退出与 Harness 选型 · 2026-09-20

- 移除旧 CLI 启动命令、专用代理路由与探测脚本；设置页改为明确的“替代运行时待接入”。没有增加第三方运行时依赖。
- 定向后端回归 **129 通过**：覆盖旧配置不能重启进程、管理接口不能重新启用、连接诊断不读凭据或启动进程、会话提前拒绝、草稿不删除且不重试、历史事件、权限与会话池兼容。
- Web 类型检查与生产构建通过；修改文档的本地链接检查、新增 Mermaid 图解析通过。自有公开分发检查、Gitleaks 文件树扫描及已知私有标识扫描（英文标识按域名／名称边界匹配）通过。详细日志保留在私有审计目录。
- OpenCode、DeepSeek Harness 和 OpenHands 只完成官方资料／接口评估；DeepSeek 源码固定提交见 [评估记录](HARNESS_REPLACEMENT.md)。未安装或运行候选引擎，没有真实模型、工具或企业网关联调证据。
- 保留平台侧历史兼容代码与设计来源说明，未宣称所有协议代码均已重写；新适配器要单独验收。
- 本节为定向验证，未重新执行上表全部用例。原始私有仓库的跟踪文件未修改。

## 文档与当前代码一致性复核 · 2026-09-20

- 以原有 97 份 Markdown 建立文档清单；修正当前入口、历史状态、创建路径、SDK 数据边界、Bridge 操作和业务示例，新增中英文复核记录。运行提示词按行为配置识别，未批量修改。
- 更新后共 99 份 Markdown 的本地文件链接检查通过，修复 3 个失效链接。
- 中英文 README、Bridge 指南与 Project SDK 指南共 **11 个 Bash 示例块**通过 `bash -n`；合成响应时长案例算式复核通过。这里只验证语法，不代表示例已执行。
- 按仓库 Bridge／节点变更检查要求，在隔离数据库运行 `tests/test_node_scheduling.py`，**58 通过**。业务代码未改，没有重新执行全部后端或前端测试。
- 自有公开分发检查与 Gitleaks 文件树扫描通过。Docker 不在本机可用，修正后的容器示例仍保留“待部署验证”，未宣称已构建或成功安装。
- API、默认数据出口、模型、节点和原私有仓库未变更；仍需代码改造的问题见 [文档复核](DOCUMENTATION_REVIEW.md)。

## 企业运行图解 · 2026-09-20

- 补充中英文组织职责、业务交付、只读 Harness 调用时序和改进决策，共 8 张图；README 引用组织总图，数据与训练流程继续以原 README 为单一说明来源。
- 8 张新增 Mermaid 图在本地 Chromium 中实际渲染为 SVG，检查文字未超出图框，并抽查组织、交付、时序和决策四类图片的排版；另解析中英文 README 现有 8 张 Mermaid 图。
- SVG 不含脚本、HTML foreignObject 或外部资源依赖；保留 Mermaid 源码、文字替代及两种语言的静态图。
- 本次仅修改文档和静态图，未运行业务测试、外部 Harness、真实模型或训练任务。图中建议流程、已有接口及待配置能力分别说明，未将图绘制完成视为集成验收。

## README 企业价值表达 · 2026-09-20

- 中英文同步补充五项价值主张、角色收益、长期资产和试点验收口径，区分预期价值与已实现业务成果。
- 核对两种语言的章节与显式锚点一致；启动、验证命令及原有 Mermaid 源码保持不变；本地链接与差异空白检查通过。
- 本次只调整文档，未运行业务回归、调用模型或改变运行配置；未新增真实收益数据或宣称完整财务核算功能。

## 产品案例与求职作品集 · 2026-09-20

- README 增加项目案例入口，中英文案例同步呈现本人已说明的建设职责、产品取舍、证据入口和简历衔接；未补造个人贡献比例、企业采用数据或模型训练结果。
- 两种语言的 README 导航与案例结构保持对应，本地链接和差异空白检查通过；原有命令、流程图和运行配置保持不变。
- 本次为文档调整，未运行新的业务回归或实际演示；案例中的演示路线不计作已配置成功的端到端验证。

## Apache-2.0 许可证配置 · 2026-09-20

- 从 Apache 官方地址获取未修改的英文许可证全文并保存为根目录 LICENSE，SHA-256 为 cfc7749b96f63bd31c3c42b5c471bf756814053e847c10f3eb003417bc523d30。
- 中英文 README、第三方声明及发布状态说明同步更新；102 份 Markdown 的本地文件链接检查通过，README 命令与 Mermaid 内容未变。
- 本次仅增加许可证和调整说明，未重新运行应用测试；许可证选择不代替权属或完整依赖许可审查，原私有仓库未修改。

最新状态以[发布前复核](RELEASE_CHECK_20260920.md)为准；历史分项记录不代表全量验收。
